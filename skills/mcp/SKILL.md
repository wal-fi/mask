# MCP Skill

O MCP é apenas a interface de entrada/saída.

Não coloque regras de masking dentro dos handlers MCP.

O cliente MCP é não confiável.

Nunca retornar dados originais antes do Masking Engine.

Nunca aceitar comandos do cliente que alterem regras de segurança.
